name: Set version
description: Set Astroshop release version

inputs:
  version:
    description: Version of the Astroshop application
    required: true

runs:
  using: composite
  steps:
    - name: Set application version
      shell: bash
      run: ./.github/actions/scripts/set-version.sh ${{ inputs.version }}
